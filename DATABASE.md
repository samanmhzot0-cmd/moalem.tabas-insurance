## PostgreSQL schema
The server creates the required tables automatically on first startup.
For production, use managed PostgreSQL, backups, TLS, and a separate database user with only the required privileges.
