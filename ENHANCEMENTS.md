# Maallem Insurance — Enhanced UI + 200-user architecture

این نسخه از فایل پروژه‌ای که فرستادید ساخته شده است.

بهبودها:
- رابط کاربری مدرن‌تر و واکنش‌گرا
- داشبورد خلاصه با تعداد درخواست‌ها و عکس‌ها
- ناوبری تب‌محور برای موبایل و دسکتاپ
- صفحه ورود/ثبت‌نام تمیزتر
- پیش‌نمایش عکس‌ها قبل از ارسال
- حفظ عکس Original و پردازش جداگانه Preview/AI در backend موجود
- دستیار بیمه فارسی/انگلیسی
- Health indicator
- نمایش وضعیت پردازش عکس
- استفاده از API موجود پروژه به جای نگهداری داده حساس در مرورگر

برای ظرفیت ۲۰۰ کاربر همزمان:
1. PostgreSQL managed با backup و SSL
2. Redis فعال برای Queue
3. Object Storage خصوصی برای عکس‌ها
4. حداقل دو Node instance پشت Load Balancer در زمان افزایش ترافیک
5. مانیتورینگ CPU/RAM/DB/Queue
6. کلیدهای API و secrets فقط در environment variables

توجه: «۲۰۰ کاربر همزمان» باید با load test روی سناریوهای واقعی (ورود، درخواست، آپلود چند عکس، پردازش AI) تأیید شود؛ عدد قطعی بدون تست بار قابل تضمین نیست.
